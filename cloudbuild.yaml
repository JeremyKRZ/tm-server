steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'compute'
      - 'ssh'
      - 'instance-1'
      - '--project=tm-server-krz'
      - '--zone=europe-west9-a'
      - '--command=cd tm-server && git pull'